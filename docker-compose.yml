version: '3.8'

services:
  ntp:
    build: .
    container_name: ntp
    ports:
      - "8080:8080"
    volumes:
      - ./data:/root/data
    restart: unless-stopped
    environment:
      - PORT=8080
